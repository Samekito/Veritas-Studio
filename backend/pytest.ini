[pytest]
# Restrict default collection to the offline unit suite. Without this, a bare
# `pytest` from backend/ would also collect test_pipeline.py (matches the
# default test_*.py glob), which hits REAL B2 + GMI APIs and COSTS MONEY.
# smoke_test.py / test_pipeline.py must be run explicitly, never by default.
testpaths = tests
