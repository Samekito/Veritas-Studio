# Veritas Studio — backend dependencies
# Genblaze umbrella ships core + B2/S3 storage; [gmicloud] adds the GMI Cloud provider.
genblaze[gmicloud]>=0.3.4
genblaze-elevenlabs

# Web API
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
pydantic>=2.7
